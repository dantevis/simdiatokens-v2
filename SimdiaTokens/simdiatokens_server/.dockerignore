# Prevent local .env and secret files from leaking into the Docker image.
# Railway injects env vars at runtime; these files are for local dev only.
.env
.env.*
.client_secret
*.local

# Build artifacts
target/
